{{-- Right-aligned action row above a hairline, as every modal in the design has. --}}
<div {{ $attributes->class('border-border mt-6 flex items-center justify-end gap-2 border-t pt-4') }}>
    {{ $slot }}
</div>
