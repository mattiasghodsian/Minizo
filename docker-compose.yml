version: "3"
services:
  minizo:
    build: .
    ports:
      - 3000:3000
    volumes:
      - ./:/srv
      - /home/user/Music:/music:rw
    user: 1000:1000 # change this to your user
    restart: always

networks:
  default:
    name: minizo
    external: true