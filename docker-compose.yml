version: "3"
services:
  minizo:
    build: .
    ports:
      - 3000:3000
    volumes:
      - ./:/srv
      - /home/user/Music:/music:rw
    restart: always

networks:
  default:
    name: minizo
    external: true