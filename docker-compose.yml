version: "3"
services:
  minizo:
    build: ./docker
    ports:
      - 3000:3000
    volumes:
      - ./:/srv
      - /home/user/Music:/music:ro
    restart: always

networks:
  default:
    name: minizo
    external: true